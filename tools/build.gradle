description = "tools that operate on the archie reference models and archetype object model"

dependencies {
  compile project(':grammars')
  compile project(':grammars2')
  compile project(':base')
  compile project(':aom')
  compile project(':bmm') 
  compile project(':path-queries')
  compile project(':utils')
  compile project(':openehr-terminology')
  testCompile project(':openehr-rm')
  testCompile project(':archie-utils')
  testCompile project(':i18n')
  testCompile project(':test-rm')
  testCompile project(':referencemodels')

  compile 'org.leadpony.justify:justify:3.1.0'
  compile 'org.glassfish:jakarta.json:2.0.1:module'
}