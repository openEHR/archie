description = "Base classes for an OpenEHR implementation"

dependencies {
    //only for the xpath grammar. perhaps include it here?
    api project(':grammars')

    api(libs.jackson.annotations)
    api(libs.jaxb.api)
    api(libs.jsr305)

    implementation(libs.guava)
}
