description = "Basic utilities for use in the archie library"

dependencies {
    api project(':base')

    api(libs.bundles.jackson)
    api(libs.kryo)

    implementation(libs.guava)
    implementation(libs.threeten.extra)
}
