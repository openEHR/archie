description = "An implementation of openehr path queries"

dependencies {
	//only for the xpath grammar. perhaps include it here?
	api project(':grammars')
	api project(':base')
	api project(':aom') //modelInfoLookup is here and should be split and moved
	testImplementation project(':openehr-rm')
}